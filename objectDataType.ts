let students:object;
students={name:'shimul',roll:9}
console.log(students)

const Students: {
  name: string;
  roll: number;
} = {
  name: "shimul",
  roll: 9,
};

console.log(Students.name);
console.log(Students.roll);